import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
        backgroundColor: "#fff",
        width: 316,
        height: 60,
        marginBottom: 20,
        borderRadius: 7,
        elevation: 4,
    },
    textButton: {
        color: "#000",
        textAlign: "center",
        marginTop: "auto",
        marginBottom: "auto",
        fontSize: 18,
    }
})